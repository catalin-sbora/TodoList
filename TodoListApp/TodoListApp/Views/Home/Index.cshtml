@using TodoListApp.UI.Models
@model TodoListHomeViewModel
@{
    ViewData["Title"] = "Home Page";
}
@{
    if (Model.TodoLists != null)
        foreach (var todoList in Model.TodoLists)
        {
            <div>
              @todoList.Title
            </div>    
            
        }
}
